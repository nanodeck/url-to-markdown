apiVersion: v1
kind: Secret
metadata:
  name: { { include "url-to-markdown.fullname" . } }
  labels: { { - include "url-to-markdown.labels" . | nindent 4 } }
type: Opaque
data:
  APP_KEY: { { .Values.appKey | b64enc | quote } }
