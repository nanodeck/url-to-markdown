{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "url-to-markdown.serviceAccountName" . }}
  labels:
    {{- include "url-to-markdown.labels" . | nindent 4 }}
{{- end }}
